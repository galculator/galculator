## Process this file with automake to produce Makefile.in

docdir = $(datadir)/doc/galculator

doc_DATA = calc_basic_internal \
		shortcuts
		
man_MANS = galculator.1

EXTRA_DIST = $(doc_DATA) \
		$(man_MANS)
